1. next((col for col in possible\_text\_cols if col in df.columns), None).    Translation: "Check the first guess. Is it in the CSV columns? No? Check the second. Yes? Stop       and grab that name."2\. A corrupted file is one where the bits on the hard drive are scrambled, so the software (like Notepad) cannot open it at all.3.possible\_label\_cols = \['label', 'generated', 'class', 'target']4\. Look at main.py (Line 33 approx).The code: if os.path.exists(full\_path): wasn't thereengine is still an object right?overlapping still not clear---* what is collections module . what does "Counter " do in collections ?* what is textstat, nltk* what does punkt, punkt\_tab, stopwords do ?* 